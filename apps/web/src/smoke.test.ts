import { describe, expect, it } from 'vitest';

/* Placeholder so the runner has work before real suites land. Delete once this
   app has genuine tests. */
describe('web', () => {
  it('is wired into the test runner', () => {
    expect(1 + 1).toBe(2);
  });
});
