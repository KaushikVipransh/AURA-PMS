/**
 * @aura/db — Prisma schema, generated client, migrations, and seed data.
 *
 * Exports a singleton PrismaClient. The org-scoping client extension (W3-06)
 * wraps it so every query filters by the actor's organization, which is the
 * structural fix for PLAN.md F-02.
 *
 * Prisma is initialised in W1-01 — see TASKS.md.
 */

export {};
