{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "AuraPMS Node — server, worker, and shared packages",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": ["node"]
  }
}
