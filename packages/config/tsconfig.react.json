{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "AuraPMS React — the Vite single-page application",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["vite/client"]
  }
}
